# Node.js 14 버전이 설치된 공식 이미지를 베이스로 사용합니다.
FROM node:14

# 현재 디렉토리의 모든 파일을 컨테이너의 루트 디렉토리로 복사합니다.
COPY ./ /

# RUN 명령어: 이미지를 빌드할 때 한 번만 실행되어, 결과가 이미지에 저장됩니다.
# 여기서는 의존성 패키지를 설치합니다.
RUN npm install

# CMD 명령어: 컨테이너가 실행될 때마다 실행되는 기본 명령을 지정합니다.
# 여기서는 컨테이너가 시작될 때 npm start 명령을 실행합니다.
CMD ["npm", "start"]